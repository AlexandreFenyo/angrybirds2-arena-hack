
Etats :
P : Play
S : Stop
T : timewarp

On commence en P

Depuis P :
On voit T et S
- clique sur S :
  mitm saute à now
  passe à S
- clique sur T :
  passe à T
bg async :
  rien : timewarp bloqué à la valeur de mitm

depuis S :
on voit P
- clique sur P :
  mitm saute à timewarp
  passe à P
bg async :
  on décrémente timewarp toutes les secondes jusqu'à bloquer sur now

depuis T :
on voit S et T
- clique sur S :
  mitm saute à now
  passe à S
- clique sur T :
  passe à T
bg async :
  on change timewarp : +1h
  mitm saute à timewarp
  on passe à P







apk add --no-cache strace
/usr/sbin/httpd -f /etc/apache2/httpd.conf -DFOREGROUND

GET /cgi-bin/index.cgi HTTP/1.1
Host: hack.com

telnet localhost 3130
GET http://d061c0b64af5/cgi-bin/index.cgi HTTP/1.1
Host: d061c0b64af5

telnet localhost 3130
GET http://hack/cgi-bin/index.cgi HTTP/1.1
Host: hack

telnet localhost 3130
GET http://hack.com/cgi-bin/index.cgi HTTP/1.1
Host: hack.com


telnet localhost 8080
GET /cgi-bin/index.cgi HTTP/1.1
Host: hack.com





<html>

  <head>
    <script type="text/javascript">
      var xhr = new XMLHttpRequest()
      xhr.onreadystatechange = function() {
      if (xhr.readyState == 4 && (xhr.status == 200 || xhr.status == 0))
      document.getElementById("msg").innerHTML = xhr.responseText
      }
      function loop() {
      xhr.open("GET", "msg.txt", true)
      xhr.send(null)
      setTimeout(loop, 500)
      }
    </script>
  </head>

  <body>
    <script type="text/javascript">
      setTimeout(loop, 500)
    </script>

    body

    <b id="msg">message</b>
   
  </body>
</html>
